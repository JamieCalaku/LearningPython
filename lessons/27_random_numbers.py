import random

random.randint(1, 6) # Returns random int from 1 to 6
random.random() # Returns random float from 0 to 1
random.choice(["rock", "paper", "scissors"]) # Chooses 1 of these 3 options
random.shuffle(["1", "2", "3"]) # Shuffles all values